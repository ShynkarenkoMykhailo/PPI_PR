{
  "name": "gha-practice-ci-cd",
  "version": "1.0.0",
  "description": "Project for GHA practical task",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "mkdir -p dist && cp index.js dist/index.js && echo 'Build artifact' > dist/build.txt",
    "test": "mocha --reporter=junit --reporter-options mochaFile=report.xml"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.0"
  }
}